


function limparInput(inputAge, inputEmail, inputName) {
    inputEmail.current.value = "";
    inputName.current.value = "";
    inputAge.current.value = "";
}

export default limparInput;